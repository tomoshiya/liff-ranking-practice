# 🎨 カスタマイズガイド

このファイルは、`index.html` を自分で編集する際の場所をメモしたものです。

---

## 📌 1. ランダムテーマの編集

### 場所
- **ファイル**: `index.html`
- **行番号**: 989～1025行目付近
- **検索ワード**: `const THEMES`

### 編集方法

```javascript
const THEMES = [
    // エモーション系
    '最近、嬉しかったことは？',
    '新しいテーマを追加',  // ← 追加する場合はこんな感じ
    
    // パーソナリティ系
    'わたしの好きな〇〇TOP5',
];
```

### 注意点
- 各行の最後にカンマ（`,`）を忘れずに
- シングルクォート（`'`）で囲む
- `//` はコメント（メモ）なので自由に編集OK

---

## 💬 2. フィードバックメッセージの編集

### A) ふたりの成績メッセージ

#### 場所
- **ファイル**: `index.html`
- **行番号**: 2548～2566行目付近
- **検索ワード**: `cooperativeMessage`

#### 現在の設定

```javascript
if (totalScore === 20) {
    cooperativeMessage = 'パーフェクト！完璧なコンビネーション！';
} else if (totalScore >= 18) {  // 19~18点
    cooperativeMessage = '素晴らしい！ほぼ完璧！';
} else if (totalScore >= 15) {  // 17~15点
    cooperativeMessage = 'いい感じ！お互いをよく知ってる！';
} else if (totalScore >= 12) {  // 14~12点
    cooperativeMessage = 'まだまだ伸びしろあり！';
} else if (totalScore >= 8) {   // 11~8点
    cooperativeMessage = '次は頑張ろう！';
} else {                        // 7~0点
    cooperativeMessage = 'もっと話そう！';
}
```

### B) ひとりあそびメッセージ

#### 場所
- **ファイル**: `index.html`
- **行番号**: 2987～3000行目、3614～3627行目付近
- **検索ワード**: `message = 'パーフェクト！'`

#### 現在の設定

```javascript
if (totalScore === 10) {
    message = 'パーフェクト！';
} else if (totalScore >= 8) {   // 9~8点
    message = 'すごい！';
} else if (totalScore >= 6) {   // 7~6点
    message = 'いい感じ！';
} else if (totalScore >= 4) {   // 5~4点
    message = 'おしい！';
} else {                        // 3~0点
    message = 'がんばろう！';
}
```

---

## ⚠️ 編集時の注意点

### ✅ 編集してOKなもの
- テーマの文字列（`'わたしの好きな〇〇TOP5'`）
- メッセージの文字列（`'パーフェクト！'`）
- 絵文字（`'🎉'`）
- コメント（`// エモーション系`）

### ❌ 触らない方が良いもの
- `const THEMES = [` ← この構造
- `if (totalScore >= 18)` ← 点数の条件（慣れたら変更OK）
- `];` や `}` ← カッコの閉じ
- その他のコード部分

---

## 🚀 編集後の手順

1. ファイルを保存
2. ターミナルで以下を実行：

```bash
cd c:\Users\rshio\Documents\liff-ranking-practice
git add index.html
git commit -m "feat: update themes and messages"
git push origin main
```

3. GitHub Pages で自動デプロイされます（数分待つ）

---

## 📝 メモ欄

自由にメモを書いてください：

- 
- 
- 

---

最終更新: 2026年1月27日
